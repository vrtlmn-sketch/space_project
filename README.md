# This is a c++ space object simulator

Make is used to manage the project

## how to get `make`

### Linux (Debian/Ubuntu)
```bash
sudo apt update
sudo apt install make

```
### then run "make run" to run
